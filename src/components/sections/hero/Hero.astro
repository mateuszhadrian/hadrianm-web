<section id="hero" class="hero">
  <div class="hero__content">
    <h1>Hero test content</h1>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 200svh;
  }

  .hero__content {
    position: sticky;
    top: 0;
    height: 100svh;
    display: grid;
    place-items: center;
    color: #fff;
    font-family: system-ui, sans-serif;
  }

  .hero__content h1 {
    font-size: clamp(1.5rem, 5vw, 3rem);
    font-weight: 700;
  }
</style>
