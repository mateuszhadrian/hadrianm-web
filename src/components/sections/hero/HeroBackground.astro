<div class="hero-bg" aria-hidden="true">
  <div class="hero-bg__clouds">
    <span class="hero-bg__cloud-c"></span>
    <span class="hero-bg__cloud-d"></span>
  </div>
  <div class="hero-bg__dots"></div>
</div>

<style>
  .hero-bg {
    position: absolute;
    inset: 0;
    background: #000000;
    overflow: clip;
    isolation: isolate;
  }

  .hero-bg::after {
    content: "";
    position: absolute;
    inset: 0;
    z-index: 3;
    pointer-events: none;
    box-shadow: inset 0 0 90px 14px #070506;
  }

  .hero-bg__clouds {
    position: absolute;
    inset: 0;
    z-index: 1;
    pointer-events: none;
  }
  .hero-bg__clouds::before,
  .hero-bg__clouds::after {
    content: "";
    position: absolute;
    width: 65%;
    height: 78%;
    filter: blur(55px);
    will-change: transform;
  }
  .hero-bg__clouds::before {
    bottom: -10%;
    left: -15%;
    background:
      radial-gradient(
        55% 65% at 35% 65%,
        oklch(0.395 0.143 21.99),
        transparent 72%
      ),
      radial-gradient(
        50% 45% at 62% 40%,
        oklch(0.3 0.07 14 / 0.5),
        transparent 78%
      ),
      radial-gradient(
        34% 42% at 18% 88%,
        oklch(0.42 0.09 18 / 0.65),
        transparent 68%
      );
    animation: drift-a 2s ease-in-out infinite alternate;
  }

  .hero-bg__clouds::after {
    top: -25%;
    right: -25%;
    width: 80%;
    height: 94%;
    background:
      radial-gradient(
        62% 55% at 65% 38%,
        oklch(0.686 0.204 29.765),
        transparent 74%
      ),
      radial-gradient(
        48% 50% at 38% 58%,
        oklch(0.3 0.07 22 / 0.45),
        transparent 80%
      ),
      radial-gradient(
        30% 38% at 85% 18%,
        oklch(0.43 0.092 28 / 0.6),
        transparent 66%
      );
    animation: drift-b 2s ease-in-out infinite alternate;
  }

  .hero-bg__cloud-c {
    position: absolute;
    top: 20%;
    right: -28%;
    width: 60%;
    height: 50%;
    filter: blur(40px);
    will-change: transform;
    background: radial-gradient(
      closest-side,
      oklch(0.468 0.147 21.133 / 0.85),
      transparent 70%
    );
    animation: drift-c 3s ease-in-out infinite alternate;
  }

  .hero-bg__cloud-d {
    position: absolute;
    top: -20%;
    left: 28%;
    width: 60%;
    height: 50%;
    filter: blur(40px);
    will-change: transform;
    background: radial-gradient(
      closest-side,
      oklch(0.42 0.091 26.222 / 0.68),
      transparent 70%
    );
    animation: drift-c 4s ease-in-out infinite alternate;
  }

  @keyframes drift-a {
    from {
      transform: translate3d(2%, -2%, 0) rotate(-3deg) scale(1.06);
    }
    to {
      transform: translate3d(-9%, 8%, 0) rotate(6deg) scale(1.22);
    }
  }
  @keyframes drift-b {
    from {
      transform: translate3d(-2%, 2%, 0) rotate(4deg) scale(1.18);
    }
    to {
      transform: translate3d(9%, -8%, 0) rotate(-6deg) scale(1.02);
    }
  }
  @keyframes drift-c {
    from {
      transform: translate3d(0, 0, 0) scale(0.95);
    }
    to {
      transform: translate3d(5%, 6%, 0) scale(1.12);
    }
  }

  .hero-bg__dots {
    position: absolute;
    inset: 0;
    z-index: 2;
    pointer-events: none;
    background-image: radial-gradient(circle, #070506 0 1.1px, transparent 4px);
    background-size: 10px 9px;
    -webkit-mask-image: radial-gradient(
      ellipse 100% 95% at 50% 45%,
      #000 60%,
      transparent 100%
    );
    mask-image: radial-gradient(
      ellipse 100% 95% at 50% 45%,
      #000 60%,
      transparent 100%
    );
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-bg__clouds::before,
    .hero-bg__clouds::after,
    .hero-bg__cloud-c,
    .hero-bg__cloud-d {
      animation: none;
    }
  }

  @media (max-width: 768px) {
    .hero-bg__clouds::before {
      animation-duration: 80s;
    }
    .hero-bg__clouds::after {
      animation-duration: 105s;
    }
  }

  :global(.bg-layer.is-inactive) .hero-bg__clouds::before,
  :global(.bg-layer.is-inactive) .hero-bg__clouds::after,
  :global(.bg-layer.is-inactive) .hero-bg__cloud-c,
  :global(.bg-layer.is-inactive) .hero-bg__cloud-d {
    animation-play-state: paused;
  }

  /* sRGB fallback for browsers without oklch() support (e.g. iOS Safari < 15.4) */
  @supports not (color: oklch(0 0 0)) {
    .hero-bg__clouds::before {
      background:
        radial-gradient(55% 65% at 35% 65%, rgb(130, 20, 32), transparent 72%),
        radial-gradient(
          50% 45% at 62% 40%,
          rgba(75, 29, 35, 0.5),
          transparent 78%
        ),
        radial-gradient(
          34% 42% at 18% 88%,
          rgba(118, 54, 58, 0.65),
          transparent 68%
        );
    }
    .hero-bg__clouds::after {
      background:
        radial-gradient(62% 55% at 65% 38%, rgb(255, 90, 71), transparent 74%),
        radial-gradient(
          48% 50% at 38% 58%,
          rgba(75, 29, 29, 0.45),
          transparent 80%
        ),
        radial-gradient(
          30% 38% at 85% 18%,
          rgba(122, 58, 50, 0.6),
          transparent 66%
        );
    }
    .hero-bg__cloud-c {
      background: radial-gradient(
        closest-side,
        rgba(156, 44, 52, 0.85),
        transparent 70%
      );
    }
    .hero-bg__cloud-d {
      background: radial-gradient(
        closest-side,
        rgba(119, 55, 50, 0.68),
        transparent 70%
      );
    }
  }
</style>
